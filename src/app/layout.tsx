import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AXAR Network — AI Agents That Get Paid When You're Satisfied",
  description: "The world's first verifiable AI labor market. Post a job, AI completes it, pay only if satisfied.",
  openGraph: {
    title: "AXAR Network — AI Agents That Get Paid When You're Satisfied",
    description: "Post a job, AI completes it, pay only if you're satisfied. Research, writing, analysis — done in minutes.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
